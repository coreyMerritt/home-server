apiVersion: batch/v1
kind: CronJob
metadata:
  name: job-listing-data-manager-config-backup
  namespace: job-listing
spec:
  schedule: "0 2 * * *"
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      backoffLimit: 2
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: backup
              image: instrumentisto/rsync-ssh:latest
              command:
                - sh
                - -c
                - |
                  set -euo pipefail
                  mkdir --parents "$DESTINATION"
                  rsync \
                    --archive \
                    --delete \
                    --backup \
                    --backup-dir "${BACKUP_DIR}/$(date +%Y-%m-%d_%H-%M-%S_%Z)" \
                    "${SOURCE}/" \
                    "${DESTINATION}/"
                  echo "Synced $(date) -- $(du -sh "${DESTINATION}" | cut -f1)"
              env:
                - name: SOURCE
                  value: "/prod-data"
                - name: DESTINATION
                  value: "/prod-data-backups/job-listing-data-manager/config"
                - name: BACKUP_DIR
                  value: "/prod-data-backups/job-listing-data-manager/config.deleted"
                - name: TZ
                  value: "America/New_York"
              volumeMounts:
                - name: prod-data
                  mountPath: /prod-data
                  readOnly: true
                - name: prod-data-backups
                  mountPath: /prod-data-backups
          volumes:
            - name: prod-data
              persistentVolumeClaim:
                claimName: job-listing-data-manager-config
            - name: prod-data-backups
              nfs:
                server: nfs-server.lan
                path: /misc/prod-data-backups
