apiVersion: v1
kind: Secret
metadata:
  name: job-listing-gui-tls
  namespace: job-listing
type: kubernetes.io/tls
data:
  tls.crt: "{{ op://personal-cli/job-listing-gui/cert-b64 }}"
  tls.key: "{{ op://personal-cli/job-listing-gui/key-b64 }}"
