apiVersion: v1
kind: Secret
metadata:
  name: grafana-tls
  namespace: observability
type: kubernetes.io/tls
data:
  tls.crt: "{{ op://personal-cli/grafana/cert-b64 }}"
  tls.key: "{{ op://personal-cli/grafana/key-b64 }}"
