apiVersion: v1
kind: Secret
metadata:
  name: ghcr
  namespace: printer
type: kubernetes.io/dockerconfigjson
stringData:
  .dockerconfigjson: |
    {
      "auths": {
        "ghcr.io": {
          "username": "coreyMerritt",
          "password": "{{ op://personal-cli/github/token }}"
        }
      }
    }
