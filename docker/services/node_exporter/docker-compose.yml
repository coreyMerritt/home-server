services:
  node_exporter:
    command:
      - '--path.rootfs=/host'
    container_name: "node_exporter"
    image: "prom/node-exporter"
    network_mode: "host"
    pid: "host"
    restart: "unless-stopped"
    volumes:
      - "/:/host:ro,rslave"
