server {
    listen 80;
    server_name job-listing-gui.lan;

    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    http2 on;

    server_name job-listing-gui.lan;

    ssl_certificate /etc/nginx/certs/job-listing-gui.lan.pem;
    ssl_certificate_key /etc/nginx/certs/job-listing-gui.lan-key.pem;

    location / {
        proxy_pass http://job-listing-gui:790;
        include /etc/nginx/snippets/proxy.conf;
    }
}
