server {
    listen 80;
    server_name prometheus.lan;

    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    http2 on;

    server_name prometheus.lan;

    ssl_certificate /etc/nginx/certs/prometheus.lan.pem;
    ssl_certificate_key /etc/nginx/certs/prometheus.lan-key.pem;

    location / {
        proxy_pass http://prometheus:9090;
        include /etc/nginx/snippets/proxy.conf;
    }
}
