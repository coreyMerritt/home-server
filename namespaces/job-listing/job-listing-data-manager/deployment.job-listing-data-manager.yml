apiVersion: apps/v1
kind: Deployment
metadata:
  name: job-listing-data-manager
  namespace: job-listing
  labels:
    app: job-listing-data-manager
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: job-listing-data-manager
  template:
    metadata:
      labels:
        app: job-listing-data-manager
    spec:
      imagePullSecrets:
        - name: ghcr
      containers:
        - name: job-listing-data-manager
          image: ghcr.io/coreymerritt/job-listing-data-manager:cors-fix-cc017945-5935-4466-b29f-9bfeef4b655e
          command: ["./run-server.sh"]
          envFrom:
            - configMapRef:
                name: job-listing-data-manager
            - secretRef:
                name: job-listing-data-manager
          ports:
            - containerPort: 7937
              name: http
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
          volumeMounts:
            - name: config
              mountPath: /etc/job_listing_data_manager
          workingDir: /app
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: job-listing-data-manager-config
