apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: tm-rabbitmq
spec:
  serviceName: tm-rabbitmq-headless
  replicas: 2
  selector:
    matchLabels:
      app: tm-rabbitmq
  template:
    metadata:
      labels:
        app: tm-rabbitmq
    spec:
      serviceAccountName: tm-rabbitmq
      containers:
        - name: tm-rabbitmq
          image: rabbitmq:3.13-management
          envFrom:
            - configMapRef:
                name: tm-rabbitmq
            - secretRef:
                name: tm-rabbitmq
          ports:
            - containerPort: 5672
            - containerPort: 15672
            - containerPort: 25672
          volumeMounts:
            - name: rabbitmq-data
              mountPath: /var/lib/rabbitmq
            - name: config
              mountPath: /etc/rabbitmq/enabled_plugins
              subPath: enabled_plugins
            - name: config
              mountPath: /etc/rabbitmq/rabbitmq.conf
              subPath: rabbitmq.conf
      volumes:
        - name: config
          configMap:
            name: tm-rabbitmq-config
  volumeClaimTemplates:
    - metadata:
        name: rabbitmq-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 2Gi
